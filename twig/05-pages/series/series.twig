{% extends '@templates/one-column.twig' %}

{% block contentTop %}
{% endblock %}

{% block pageContent %}
  <div class="ama__series">
    <div class="container">
      <span class="series-icon">AMA Series</span>
      {#Then I should see the heading "Series Title" in the masthead (h2)#}
      {#And I should see the "Social share" in the masthead#}
      {% set masthead = series.masthead %}
      {% include "@organisms/masthead/masthead.twig" %}
    </div>

    {#And I should see the "Series Page Index"#}
    <div class="container">
      {% set seriesIndex = series.seriesIndex %}
      {% include '@organisms/series-index.twig' %}
    </div>
  </div>
{% endblock %}
